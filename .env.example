# Copy to .env and fill in values. Never commit .env.

# Optional: pre-fill connection settings so technicians don't need to type them each time.
# Leave blank if each user enters their own credentials in the UI.
ORCANOS_ENDPOINT=https://app.orcanos.com/YOUR_ACCOUNT/api/v2/Json
ORCANOS_AUTH_KEY=
